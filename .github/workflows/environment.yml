#Environment is a deployment target such as Dev, Test, or Production. It can be protected using approvals and deployment rules before a workflow can deploy.

name: Deploy App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Deploying Application..."
