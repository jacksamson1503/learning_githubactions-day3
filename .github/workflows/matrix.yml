name: Matric Demo

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        env: [dev, test, prod]

    steps:
      - name: show Environment
        run: |
          echo: "Deploying to ${{ matrix.env }}"
