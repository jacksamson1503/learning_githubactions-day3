name: nodejs.versions

on: 
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: [ 18, 20, 22 ]

    steps:
      - name: Testing application
        run: |
         echo "Testing an appliaction on ${{ matrix.env }}"
